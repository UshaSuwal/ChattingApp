<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">Turbochat</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
    <ul class="navbar-nav">
      <% if current_user %>
        <li class="nav-item">
          <div class="links">
            <%= link_to edit_user_registration_path, class: "btn btn-outline-light mr-3" do %>
              <i class="fas fa-user-edit"></i> Edit Profile
            <% end %>
          </div>
        </li>
        <li class="nav-item">
          <div class="links">
            <%= link_to destroy_user_session_path, method: :delete, class: "btn btn-outline-light", data: { turbo_confirm: 'Are you sure?', "turbo-method": :delete } do %>
              <i class="fas fa-sign-out-alt"></i> Sign Out
            <% end %>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <div class="links">
            <%= link_to new_user_registration_path, class: "btn btn-outline-light" do %>
              <i class="fas fa-user-plus"></i> Sign Up
            <% end %>
          </div>
        </li>
        <li class="nav-item">
          <div class="links">
            <%= link_to new_user_session_path, class: "btn btn-outline-light" do %>
              <i class="fas fa-sign-in-alt"></i> Log In
            <% end %>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
